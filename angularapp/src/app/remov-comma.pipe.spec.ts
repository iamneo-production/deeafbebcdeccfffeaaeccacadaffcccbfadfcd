import { RemovCommaPipe } from './remov-comma.pipe';

describe('RemovCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
